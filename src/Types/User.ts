export type User = {
	id: string;
	phone: string;
	terms: boolean;
	name: string;
	email: string;
	complete: boolean;
};

export type DashboardUser = {
	id: string;
	username: string;
	role: string;
};
